name: Deploy Streamlit App

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements_web.txt
    
    - name: Test basic functionality
      run: |
        python -m pytest tests/test_basic.py -v
    
    - name: Test Streamlit app syntax
      run: |
        python -c "import streamlit; print('Streamlit import successful')"
        python -c "exec(open('app.py').read())" || echo "App syntax check completed"

  # Uncomment for Streamlit Cloud deployment
  # deploy:
  #   needs: test
  #   runs-on: ubuntu-latest
  #   if: github.ref == 'refs/heads/main'
  #   
  #   steps:
  #   - name: Deploy to Streamlit Cloud
  #     run: echo "Deploy to https://share.streamlit.io/"